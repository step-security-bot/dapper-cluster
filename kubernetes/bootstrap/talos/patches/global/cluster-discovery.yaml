cluster:
  discovery:
    registries:
      kubernetes:
        disabled: true
      service:
        disabled: false
  apiServer:
    extraArgs:
      feature-gates: AuthorizeNodeWithSelectors=false

